# Universal Cursor Rules Mirror

Public mirror of Cursor rules from:
- Project rules (`rules/project/*.mdc`)
- Global user rules (`rules/global/*.mdc`)

## Path policy
- Absolute local paths are rewritten to repo-relative paths.
- Global complexity rule is referenced as `./rules/global/algorithm-complexity-selection.mdc`.

## Notes
- This mirror is documentation/policy content only.
- Source projects may reference skills, docs, and repos not mirrored here.
