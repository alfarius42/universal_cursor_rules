---
description: Экономия токенов — загрузка документов и контекста
alwaysApply: true
---

# Экономия токенов

## Порядок обращения к контексту

1. **Сначала — основной документ по проекту.** Главный источник знаний: `src/Полная спецификация продукта - основной документ.md`. Подгружать только нужные разделы (файл большой); искать по оглавлению или ключевым словам.
2. **Затем — индекс документации.** При необходимости — `CURSOR_CONTEXT.md`, `docs/active/DOCUMENTATION_INDEX.md` или `src/docs/`; подгружать только нужные документы. Для сценариев **«как пользователю…»** — `docs/active/USER_GUIDE.md` (язык документа — русский).
3. **Не загружать всё сразу.** Не дублировать в контексте уже открытые/упомянутые документы.
4. **Фрагменты вместо целых файлов.** Для больших файлов — только нужный раздел (offset/limit или цитата).
5. **Дальше:** семантический поиск по репозиторию только если в индексе/документации не нашли.

## Приоритет контекста

- Сначала: правила (`.cursor/rules/`), релевантный раздел основного документа (`src/Полная спецификация продукта - основной документ.md`), `CURSOR_CONTEXT.md`, релевантный кусок спецификации/задачи.
- По требованию: полные гайды из `docs/active/` или `src/docs/`.
- Избегать: `node_modules`, `build/`, бэкапы, архивную документацию — не включать в контекст без необходимости.

## Ответы и рассуждения

- **Рассуждения** — ограничивать объём необходимым минимумом; не разворачивать очевидное.
- Краткие ответы; списки вместо длинных абзацев.
- Ссылки на файлы/разделы вместо копирования больших блоков.
- При повторяющихся вопросах — отсылка к уже описанному в документации.
- Формулировать ответ в требуемом объёме и формате.
